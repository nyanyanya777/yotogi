# ネットロア — 演出・技術アイデアメモ

## 文字化け演出（mojibake glitch）— 2026-06-06 オーナー発案
ホラー演出として「文字化け」をわざと出す。

### やりたいこと
1. **タグ選択画面**: 定期的に（ランダム低確率で）選択タグ等のテキストが一瞬／一定時間**文字化け**する演出。
2. **生成結果**: 約**10回に1回**のアクセス時に、生成した怪談（題名 or 本文）が**文字化けして表示される**演出。

### 技術的実現性: 容易（◎）
- `garble(text, ratio)` 関数で各文字を一定割合だけ化け字に置換。化け字セットは「Shift_JIS誤読っぽい」見た目（例: 縺/繧/繝/蜉/�/□ や CJK互換/罫線記号のランダム）でリアルにする。
- 発火制御: シード付きRNG＋確率ゲート。
  - タグ選択: レンダー時 or 一定間隔で ~5–10% のタグに適用。`flicker`で数百ms後に正規へ復帰させると「視界の端でゾッ」になる（CSSアニメ/タイマー）。
  - 生成結果: ロード時に1/10で全体 or 題名を化けさせる。タップ/スクロールで正規へ復元する「隠し」にするか、化けたまま残すかは要デザイン判断。
- 実装位置: `<Garble>` 専用コンポーネント＋フラグ。SSR/CSRどちらでも可（OGP共有カードには出さない or 別扱いが無難）。

### 重要な技術的注意（混同防止）
- **本物のエンコーディング不具合（実際にあった Shift_JIS 誤デコード）と、この意図的演出を絶対に取り違えない**こと。
  - 演出は専用コンポーネント/フラグ（例 `intentionalGlitch`）に隔離し、データ層・取得層では一切化けさせない（コーパスやAPI応答は常に正しいUTF-8）。
  - 文字化け検知の回帰テスト（CJK含有率チェック等）は、この演出コンポーネント配下を**ホワイトリスト除外**する。さもないと演出が「本物のバグ」に見え、逆に本物のバグが「演出」で見逃される。
- 共有/アクセシビリティ: 化けたままの共有は混乱を招くので、共有テキスト・スクリーンリーダー向けには正規テキストを保持（aria-labelに原文）。
