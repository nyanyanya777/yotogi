# ネットロア — 4画面 UX/UI仕様（Pencil実装用 / v2 黒・タイトル主役）

実装者向け。全画面共通: 背景は漆黒、面はほぼ出さず**余白＝闇が主役**。古血`#8E1B12`は通算2点のみ（作成の「紡ぐ」点灯／表示画面の一点）。タイトル文字が常に画面の主役、UIパーツはその脇に静かに。

## 0. 全画面を貫く原則
- **黒が9割、文字が1割、血は点**。面(鈍色`#17171B`)は塗りでなく罫`#26262B`1pxの縁取りが基本。
- **タイトル＞本文＞操作**の序列を全画面で守る。ボタンは罫線＋骨白文字、塗らない。
- アニメは「滲む・沈む・浮く」のみ。点滅・スライド・バウンス禁止。フェードは遅く(600–900ms)。
- 全画面最背面に極微グレイン＋四隅ビネット(中心`#0C0C0E`→四隅`#050506`)。

## 1. トップ画面 — 禁書の入口
役割: 最初の数秒で「静かに怖い場所」と理解させる。題字「ネットロア」＋闇に浮かぶ怪談タイトル目録で世界観を運ぶ。
```
        Netlore            ← 欧文ロゴ(細セリフ・小・灰)
      ネ ッ ト ロ ア         ← 題字(特大・明朝・骨白) ★主役
  あなたの選んだ三つから、
  まだ誰も読んでいない怪談が生まれる。   ← タグライン(セリフ・灰・小)

        ［ 怪談を紡ぐ ］      ← 開始導線(罫線ボタン・骨白)
  ── 目録 ──────────
   呼ばれ子 / 山の子守唄 / 井戸を覗くな / 七人目 / 振り向くな / 水底の母  ← 縦リスト・闇に浮く文字
```
階層: 題字＞目録＞タグライン＞［怪談を紡ぐ］＞Netloreロゴ。血は使わない(入口は静寂)。
遷移: ［怪談を紡ぐ］→作成画面・通常(遅いフェード)。

## 2. 作成画面・通常 — 三つを選ぶ
役割: モチーフを3つ選ぶ唯一の操作画面。選んだ3つが手元に集まる手応え。3つ揃うと「紡ぐ」が血の一点で静かに点る。
```
  ネットロア            ［ 解説する ］   ← 上部:小ロゴ(左)/解説トグル(右・罫線・小)
   闇から、三つを選べ。                  ← 画面見出し(明朝・中・骨白)★
   その三つが、まだ無い怪談の核になる。   ← 補足(灰・小)
   [山と森][祟りと復讐][子ども][都市]
   [時間のずれ][見てはいけない][水と井戸][七人目][呼ばれる]  ← 罫線セルのグリッド
  ─────────────────────
  選んだ三つ:  山と森 / 祟りと復讐 / —        ［ 紡ぐ ］   ← 下部固定トレイ＋実行
```
選択挙動: セルは罫線＋骨白文字(塗らない)。選択=左端に古血の縦線2px＋文字明化＋わずかな沈み(アイコン不使用)。上限3つ、超過操作は「闇に沈む(暗化)」で示す(赤エラー/トースト禁止)。［紡ぐ］は3つ揃うまで灰で不活性→揃うと古血の細縁＋骨白で点灯(この画面唯一の血)。
コピー: 見出し「闇から、三つを選べ。」補足「その三つが、まだ無い怪談の核になる。」トグル「解説する」トレイ「選んだ三つ」実行「紡ぐ」。
遷移: ［解説する］→解説状態(オーバーレイ)。［紡ぐ］→生成中の間(黒画面に題字が滲む「いま、書き下ろしています。」)→表示画面。

## 3. 作成画面・解説 — なぜ三つなのか
役割: 「何を選ぶと何が起きるか」を通常画面と地続きの2状態で。別ページに飛ばさない。オーバーレイが降り背後の選択画面は約20%まで沈めて透ける(ぼかしでなく暗化)。
```
  ネットロア            ［ 閉じる ］   ← トグルが同位置で「閉じる」に変化
  ▓▓ 背後の選択画面が闇に沈んで透ける ▓▓
    ── なぜ、三つなのか ──            ← 解説見出し(明朝・中・骨白)★
   ① モチーフは、話の核。  あなたが選ぶのは結末ではなく、話が生える種です。
   ② 三つが、交わる。      一つでは平凡な怖さも、三つ重なると、まだ無い話になる。(例: 水と井戸 × 子ども × 呼ばれる)
   ③ 同じ三つでも、毎回ちがう。 そのたびAIが新作を一本だけ書き下ろします。
        ［ 選びに戻る ］
```
挙動: 3ブロックで①核②交わり③一回性を文字だけで語る(アイコン/図解矢印禁止＝マッピング誤認回避)。例示は実モチーフ語の掛け合わせ。背後の選択状態は保持。血は使わない。
遷移: ［選びに戻る］/［閉じる］→通常へ(選択保持)。

## 4. ネットロア表示画面 — 読む
役割: 生成された怪談を静かに読み切る。タイトルが画面の絶対的主役。読後の余韻に再生成/シェアをそっと。**この画面で唯一最後に血が一点滲む**(題名脇の極小の印/染み)。
```
            井戸を覗くな               ← 怪談タイトル(特大・明朝・骨白)★★絶対主役
                              ・       ← 古血の一点(極小)。唯一の血
        山と森・水と井戸・呼ばれる       ← 核の三モチーフ(極小・灰・中央)
   ──────────────────
   その井戸は、村のいちばん奥にあった。
   誰も水を汲まないのに、夏になると底から、子どもの声がした。 …全文(セリフ・骨白・行間広め・可読幅~640px・縦スクロール)
   ──────────────────
   ［ もう一度紡ぐ ］  ［ 写し取る ］     ← 読後導線(罫線・骨白・控えめ・横並び)
```
階層: 怪談タイトル＞古血の一点＞本文＞三モチーフ表示＞［もう一度紡ぐ］/［写し取る］。背景は最暗`#050506`寄りで文字を立たせる。タイトルは本文より遥かに大きく、単独で大きな余白を持たせる。
遷移: もう一度紡ぐ→同じ三つで再生成。写し取る→共有(題名＋一文＋Netloreの印)。

## 5. 共通デザイントークン
色: bg/void `#050506`(最背面/表示背景) ・ bg/base `#0C0C0E`(基調) ・ surface/raise `#17171B`(面・最小) ・ line/lead `#26262B`(罫/縁/枠) ・ text/bone `#E9E7E1`(主文字/タイトル/本文) ・ text/ash `#8C8B92`(副/非活性) ・ accent/oldblood `#8E1B12`(血・通算2点)。塗りは極小、UIは文字＋1px罫で作る。
タイポ: 怪談タイトル(表示)=明朝/セリフ 48–72px相当・字間+8〜12% ／ 題字ネットロア(トップ)=40–60px ／ 画面見出し(作成/解説)=24–32px ／ 本文=読みやすいセリフ16–18px・行間1.9–2.1 ／ 副文/タグライン/モチーフ語=12–14px ／ 欧文Netlore=細セリフ12–16px。丸ゴシック禁止、筆/かすれ多用禁止。
余白: 単位8px。セクション間48–96pxと広く。外周マージン広め、本文最大幅~640px中央寄せ。
角丸: 0–2px(ほぼ直角)。大角丸禁止。
罫/段差: 罫1px `line/lead`。段差は影でなくbgの明度差。ドロップシャドウ禁止。ビネット＋微グレイン全画面。
モーション: フェード(600–900ms)/滲み/沈み浮きのみ。点滅/スライド/バウンス禁止。
コンポーネント: ボタン=罫線矩形＋骨白文字(塗りなし、活性頂点の紡ぐのみ古血細縁)。モチーフセル=罫線矩形＋骨白文字、選択は左端血線2px＋沈み。タイトル/目録=囲いなしの浮く文字。オーバーレイ=ぼかしでなく暗化。
共通ダミー: 怪談タイトル=呼ばれ子/山の子守唄/井戸を覗くな/七人目/振り向くな/水底の母。モチーフ語は別チーム策定中のダミー(差し替え可能な構造に)。
